# Persistent volume: SQLite database (solo.db) + optional GeoIP/Tor files.
# Drop GeoLite2-ASN.mmdb / GeoLite2-Country.mmdb / tor-exit-nodes.txt here to
# enable IP enrichment. The app starts fine without them (enrichment returns null).
